[project]
name = "osfingerprinting"
version = "0.1.0"
description = "Passive network sniffing into per-device info mapping in local db"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
    "scapy>=2.7.0",
]
